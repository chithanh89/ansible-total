- name: Install Server Basics
  apt: name={{ packages }} state=present update_cache=yes

- name: insert/update "LC_ALL" configuration block
  blockinfile:
    path: ~/.bashrc
    block: |
      LANGUAGE="en_US:"
      LC_ALL="en_US.UTF-8"
      LC_CTYPE="en_US.UTF-8"

- name: Export LC_ALL
  shell: source ~/.bashrc
  args:
    executable: /bin/bash
